# 阶段 7 测试记录：事件、周期和技术阶段

日期：2026-09-04

## 自动化测试

- 阶段 7 核心聚焦：`python -m unittest discover -s tests -p test_events_cycles.py -v`，4 项通过。
- 阶段 7 界面聚焦：`python -m unittest discover -s tests -p test_events_page.py -v`，2 项通过。
- 全量回归：`python -m unittest discover -s tests -q`，75 项通过。
- 编译检查：`python -m compileall -q app tests`，通过。
- 差异检查：`git diff --check`，通过。

覆盖内容：

- 阈值上穿事件检测具有确定性。
- 缺失值会切断连续数据段，缺失后第一条有限值不被当作跨越证据。
- 周期只配对相同规则、相同连续数据段内的开始和结束事件。
- 帧号与时间精确往返查询，非精确时间不进行插值。
- 人工调整追加 JSONL 历史并覆盖当前有效事件，原检测记录保留。
- 事件页使用滚动容器，检测在线程中执行，项目切换清空旧结果。

## 真实样本只读验收

样本：`D:\test\data\8.12\走路\pose-3d\test1_P0_1-739.trc`

- 解析帧数：739。
- 指标列：`Hip.speed`。
- 有限速度样本：739。
- 中位数阈值：约 `0.401765 m/s`。
- 上穿事件数：68，首个事件帧：50。
- 本验收只读样本目录，没有写入或修改 `D:\test\data`。

## 已知限制

- 阶段 7 当前页面接收内存中的 `MetricTable`；运动学页与事件页之间的持久化指标报告和跨页面选择联动属于阶段 8 报告闭环。
- 页面支持单条人工事件调整和历史追加；批量事件编辑及技术阶段模板在后续阶段扩展。
