# 阶段 2 实施测试记录

## 状态

阶段 2 的 Task 7～9 已完成并提交。当前分支为 `codex/phase-2`，等待远程仓库验收合并。

## 环境

- 日期：2026-09-04
- 工作区：`D:\CODEX\2026-09-02\d-codex-2026-09-01-ni-2`
- Python：项目 `.venv\Scripts\python.exe`，Python 3.12
- 已安装并验证：PySide6、NumPy、OpenCV、PyInstaller
- 测试体系：标准库 `unittest`
- Qt 测试：`QT_QPA_PLATFORM=offscreen`
- 原始项目：未访问、未写入

## 已完成提交

- `96eb395`：可折叠导航、滚动容器、可调工作区分栏和任务状态栏
- `af39174`：后台多相机帧服务、有界 LRU、项目 generation 隔离和 Qt heartbeat
- `b5484a5`：二维修正页面、语义 Pose JSON 编辑、审计历史、首次备份、恢复、撤销/重做和选择性重跑白名单

## 验证命令与结果

| 命令 | 结果 |
|---|---|
| `.venv\Scripts\python.exe -m unittest tests.test_frame_provider -v` | 通过，3 项 |
| `.venv\Scripts\python.exe -m unittest tests.test_correction_model tests.test_correction_history tests.test_correction_session tests.test_correction_rerun -v` | 通过，9 项 |
| `.venv\Scripts\python.exe -m unittest tests.test_correction_page tests.test_gui_shell -v` | 通过，6 项 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -q` | 通过，38 项 |
| `.venv\Scripts\python.exe -m compileall -q app tests` | 通过 |

## 已验证能力

- 1120×720 使用可调工作区分栏，620×480 使用右侧滚动容器访问密集控件，底部保存操作保持可见。
- `VideoCapture` 只在解码线程运行；四路预取连续 2 秒期间 Qt heartbeat 没有超过 250 ms 的事件循环停顿。
- 项目切换会清空缓存并丢弃旧 generation 的结果；导航请求会取消尚未开始的低优先级预取。
- 二维点位通过语义人物 ID 和关节点名称定位；人工移动默认写入置信度 1.0。
- 首次保存创建不可覆盖备份；后续保存不覆盖首份备份；原子替换失败时工作 JSON 保持可读。
- JSONL 损坏记录会报告行号；恢复同时恢复坐标和置信度，并追加 `source=restore` 历史，不删除旧历史。
- 取消导航不会静默丢弃未保存修改；撤销/重做同时恢复坐标和置信度。
- 选择性重跑仅使用 `triangulation`、`filtering`、`markerAugmentation`、`kinematics`，不调用 `poseEstimation`；依赖图下游报告阶段仍会标记为 stale。

## 已知限制

- 真实 Pose2Sim/Caliscope 算法尚未在本阶段执行；外部工具仍由后续阶段接入真实输入。
- 真实 `D:\test\test` 副本验收和 EXE smoke 尚未执行，按计划在工程化和最终验收任务中完成。
- 当前二维页面已完成布局、目标详情和保存入口；质量报告问题队列的真实报告加载、同步映射驱动的原视频帧定位将在后续阶段接入。
