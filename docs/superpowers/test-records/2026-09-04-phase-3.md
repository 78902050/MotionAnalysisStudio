# 阶段 3 实施测试记录

## 状态

阶段 3 的任务 10 已完成并提交到 `codex/phase-3`。

## 已完成能力

- 标定文件以 UTF-8 JSON 读取，内容指纹用于区分变化和相同文件。
- 导入只把文件副本原子写入项目 `calibration/source`，不修改外部标定源文件。
- 相同内容重复导入返回 `changed=false`，不重复失效阶段；内容变化更新当前激活文件和清单元数据。
- 损坏文件、缺失相机、无激活文件和目标写入失败均返回明确错误或 blocking 诊断。
- 逐相机诊断展示相机 ID、重投影误差、覆盖率和项目配置缺失情况。
- 主窗口的相机标定页面提供导入、当前激活文件、内容指纹和诊断列表。

## 验证命令与结果

| 命令 | 结果 |
|---|---|
| `.venv\Scripts\python.exe -m unittest tests.test_calibration_import tests.test_calibration_diagnostics -v` | 通过，4 项 |
| `.venv\Scripts\python.exe -m unittest tests.test_calibration_page tests.test_gui_shell -v` | 通过，5 项 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -q` | 通过，43 项 |
| `.venv\Scripts\python.exe -m compileall -q app tests` | 通过 |

## 已知限制

- 当前诊断读取项目约定的标定 JSON 字段，不调用或修改 Caliscope 安装目录中的程序。
- 相机内外参的几何重投影计算尚未接入，当前报告展示输入文件提供的诊断字段；真实算法适配按后续阶段计划执行。
- 真实 `D:\test\test` 副本验收和 EXE smoke 尚未执行。
