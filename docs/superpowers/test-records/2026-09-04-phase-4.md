# 阶段 4 实施测试记录

## 状态

阶段 4 的任务 11 已完成，包含数据驱动同步映射、人工偏移保存和二维修正页的动态相机接入。

## 已完成能力

- 同步帧到原视频帧的映射来自项目 `synchronization/mapping.json`，支持逐帧表、时间戳结果和分段偏移。
- 未找到映射时显示阻塞原因，不按相机名称隐式生成偏移或身份。
- 人工偏移保存到项目 `synchronization/overrides.json`，不覆盖外部映射源。
- 同步页面明确区分同步帧、原视频帧、映射方法和映射来源。
- 二维修正页面的相机列表来自项目清单，不再写死相机名称；视图回调按相机属性匹配。
- 项目 `.venv` 已安装 Pose2Sim 0.10.49、Caliscope 0.11.6 和 OpenSim 4.6；`pyproject.toml` 提供同版本 `external` 可选依赖。

## 真实样本只读检查

`D:\test\data` 已确认包含 8.12、8.14、8.15、8.18、8.19、8.20 六个批次。每个批次包含四路 `cam01`–`cam04` 的 Pose2Sim JSON、`pose-sync`、`pose-associated`、`pose-3d` 和 `kinematics` 输出；标定文件位于批次根目录。检查未向该目录写入文件。

## 验证命令与结果

| 命令 | 结果 |
|---|---|
| `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_synchronization*.py" -v` | 通过，5 项 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_gui_shell.py" -v` | 通过，4 项 |
| `.venv\Scripts\python.exe -m unittest discover -s tests -q` | 通过，48 项 |
| `.venv\Scripts\python.exe -m compileall -q app tests` | 通过 |
| `.venv\Scripts\python.exe -m pip check` | 通过，无破损依赖 |
| Pose2Sim、Caliscope、OpenCV、NumPy 导入检查 | 通过 |

## 已知限制

- `D:\test\data` 当前只完成外部结果结构的只读检查，尚未复制到临时工作区执行完整导入、修改、恢复和重跑闭环。
- 真实样本的四路 MP4 位于 Pose2Sim `pose` 输出目录；原始拍摄 MP4 未在该目录中发现，不能把输出视频当作原始视频验收。
- `python -m Pose2Sim` 不是该发行包支持的入口；安装生成的 `Pose2Sim.exe` 使用其发行包的 `GUI.main:main` 入口。
- 阶段 4 尚未执行 EXE smoke 和真实项目副本闭环；这些门禁按后续阶段计划保留。
