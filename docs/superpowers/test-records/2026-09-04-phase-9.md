# 阶段 9 测试记录：工程化、Windows 打包与最终验收

日期：2026-09-04
代码提交：`772af56 release: complete motion analysis studio rebuild`、`659ef6a fix: make packaged smoke checks reliable`

## 实现范围

- 增加项目相对路径的 Windows PyInstaller 构建脚本和发布 EXE smoke 脚本。
- 增加诊断包创建能力：仅收集脱敏清单、运行时能力、版本和小型日志，不复制 pose、视频或 TRC 输入。
- 增加 Python、PySide6、NumPy、OpenCV、PyInstaller、Pose2Sim 和 Caliscope 的安装能力检查。
- 冻结 EXE smoke 只检查实际内置的 Python/PySide6；完整安装检查仍检查外部分析和构建能力。
- smoke 脚本等待 one-file EXE 及其子进程结束，并检查真实退出码。
- 增加用户指南、阶段验收测试和能力边界回归测试。

## 环境

- Python 3.12.13
- PySide6 6.11.2
- NumPy 2.5.2
- OpenCV 5.0.0.93
- PyInstaller 6.22.2
- Pose2Sim 0.10.49
- caliscope 0.11.6
- OpenSim 4.6

## 自动化验证

```text
python -m unittest discover -s tests -q
Ran 88 tests ... OK

python -m compileall -q app tests
通过

git diff --check
通过
```

新增覆盖包括：诊断包脱敏和输入排除、项目相对路径构建、冻结能力分层、smoke 等待和退出码、版本启动入口及阶段验收接口。

## Windows EXE 验证

构建命令：

```text
scripts\build_windows.ps1
```

结果：构建成功。

输出：`outputs\build\dist\MotionAnalysisStudio.exe`

smoke 命令：

```text
scripts\smoke_exe.ps1
```

结果：`MotionAnalysisStudio.exe smoke test passed`，退出码为 0。

smoke 结束后按进程名和命令行检查发布/调试 EXE，残留进程数为 0。

## 样本与原始数据保护

- `D:\test\data` 作为只读样本目录检查，未写入或修改。
- 计划指定的 `D:\test\test` 当前不存在，因此无法执行真实项目复制、二维保存、恢复和选择性重跑的完整门禁。
- 未对任何原始 MP4、样本 pose JSON 或 TRC 文件执行写操作。

## 已知限制

- 由于 `D:\test\test` 不存在，真实项目副本全流程和原目录前后时间戳对比尚未执行；需要提供真实项目目录后补测。
- 当前 EXE smoke 验证的是启动能力，不代替带真实项目的“质检问题→二维修正→保存”人工验收。
- Pose2Sim 和 Caliscope 作为外部能力检查，不被当前 one-file 桌面启动包强行嵌入；运行分析流程时仍需使用已安装环境。
