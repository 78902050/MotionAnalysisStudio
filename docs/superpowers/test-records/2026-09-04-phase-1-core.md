# 阶段 1 核心实施测试记录

## 状态

实现中：Task 1～6 已完成并提交；阶段 1 的 GUI 壳、主线程 heartbeat 和 EXE smoke 仍按计划由后续 Task 7/16 验收。

## 环境

- 日期：2026-09-04
- 工作区：`D:\CODEX\2026-09-02\d-codex-2026-09-01-ni-2`
- 分支：`codex/phase-1`
- Python：`C:\Users\梁御祺\.agent-reach-venv\Scripts\python.exe`
- 项目版本：`0.1.0`
- 测试体系：标准库 `unittest`
- 原始项目：未访问、未写入
- 源码归档：D:\\CODEX\\2026-09-02\\motion-analysis-studio-phase-1-core-f3ee768.zip，解压副本重新通过 unittest 20 项和 compileall

## 已完成提交

- `9cfeb0d`：项目骨架和测试入口
- `9d1a81e`：语义地址和阶段图
- `46afdae`：项目清单 v3 与 v2 迁移
- `aca674c`：原子存储、JSONL 和事务恢复
- `be35c14`：后台任务中心和第三方适配器
- `316d69c`：三维质量审计

## 验证命令与结果

| 命令 | 结果 |
|---|---|
| `python -m unittest tests.test_bootstrap -v` | 通过 |
| `python -m unittest tests.test_domain_contract -v` | 通过，5 项 |
| `python -m unittest tests.test_project_manager -v` | 通过，3 项 |
| `python -m unittest tests.test_atomic_storage -v` | 通过，4 项 |
| `python -W error::ResourceWarning -m unittest tests.test_atomic_storage -v` | 通过，4 项 |
| `python -m unittest tests.test_task_center tests.test_adapters -v` | 通过，4 项 |
| `python -m unittest tests.test_quality_audit -v` | 通过，3 项 |
| `python -m unittest discover -s tests -q` | 通过，20 项 |
| `python -m compileall -q app tests` | 通过 |

## 已验证能力

- 新项目生成 schema v3 清单、中文路径和项目工作目录。
- v2 项目迁移幂等，保留 `manual_pose_edits` 和已完成阶段。
- 原子 JSON 替换失败时旧文件保持可读；首次备份不覆盖；损坏 JSONL 报告行号。
- 任务结果带项目 ID 和 generation；取消可协作结束；Pose2Sim 阶段使用允许列表并保存 stdout/stderr。
- 质量报告区分实际人数、2D 检测人数、关联人数和轨迹段数量。
- 质量问题使用相机、时间轴、帧、语义人物和关节点名称定位；缺失层生成 blocking 问题。
- 质量报告同时保存 current 和版本化 history。

## 已知限制

- 当前没有 PySide6 页面、可调分栏或四路视频 heartbeat；这些属于后续 GUI/媒体任务。
- 当前 Pose2Sim/Caliscope 适配器只提供最小只读输入和进程边界，不执行真实算法。
- 当前质量审计读取项目约定的 JSON 结果层；重投影值来自输入结果，不在本任务内重新进行相机投影计算。
- 真实 `D:\test\test` 副本验收和 EXE 启动验收尚未执行；源码归档解压回归已通过。

