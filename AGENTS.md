# HERO 项目工作约束

本项目采用 HERO-Anti-OverDefense 的规则块作为补充约束。项目中已经明确提出的安全、迁移、验证、测试和代码审查要求优先于下面的范围控制规则。

```text
=== 范围约束（约束你提议什么修法，不约束你找什么）===

1. 不要因为范围有限就漏报真实问题。
   范围限制约束的是提议的修复方式，不约束你报告通过支持的用法、真实数据或仓库代码可到达的问题。

2. 不要添加没有运行时价值的哈希、校验和或指纹。
   只有当它能替代明显更昂贵的操作，并且会改变后续动作时才添加。
   例如：用于跳过大文件的重复解析、避免重复导入；不为了“看起来更安全”而增加无意义的哈希。

3. 不要为不会发生的情况搭建防御性脚手架。
   先验证问题是否通过支持的输入、真实数据或现有代码路径可达；不可达的理论攻击面不扩展实现范围。

4. 不要为没有证据的极端场景增加复杂度。
   除非它能通过支持的用法、文档化输入或真实数据触发，否则不专门处理异国编码、符号链接竞态、RTL 排版、毫秒级竞态等情形。

5. 需要判断的地方直接判断，不用评分表、清单或重复复核取代判断。
   测试和验收用于发现具体缺陷，不用于制造形式上的“完成感”。

6. 本规则不覆盖明确要求的安全、迁移、验证和审查工作。
   用户、项目规范或系统要求的内容必须执行，即使它们看起来属于额外防御。

7. 交付物不是防御过程的记录。
   限制和风险集中写在一个“已知限制”区域；最终交付不包含过程辩护、生成痕迹或 AI 标记。

8. 保持推进。
   在合理选项中直接选择；只有当选项确实会导致不同结果且难以逆转时才暂停请求选择。

9. 最终交付物、注释、文档中的实现说明、提交信息和总结不写生成/修正过程痕迹或 AI 标记。

在运行任何检查前，先说明它要检测的具体失败，以及失败后会改变什么；不要运行没有决策价值的检查。
```

实施时的判断顺序：

1. 先确认需求是否来自当前阶段规范、真实数据或可达代码路径。
2. 若要增加校验、缓存、指纹、兼容层或抽象，说明它替代的昂贵操作和改变的后续行为。
3. 若只是为了覆盖假设中的未来需求，则不加入。
4. 对明确要求的测试、迁移、安全和验收，按项目规范执行。
